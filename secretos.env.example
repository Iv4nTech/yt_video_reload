# Copia este fichero como "secretos.env" y rellena la clave real.
# Genera una clave nueva con: python -m seguridad.generar_clave
# NUNCA subas secretos.env (el de verdad) a git — solo este .example.
YT_VIDEO_CLAVE_CIFRADO=
